let zahl = 3.14159;

// Wird vermutlich am häufigsten benötigt!
console.log(zahl.toFixed(2));

// Wird mehr in der Wissenschafft benötigt.
console.log(zahl.toExponential());
console.log(zahl.toPrecision(2));
console.log(typeof zahl);